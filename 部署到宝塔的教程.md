# Workbench 工作台 - 宝塔面板（BT-Panel）部署教程

本文档介绍如何将 **Workbench 个人工作台**（React 19 前端 + Node.js Express 后端 + SQLite 数据库）部署到安装了宝塔面板（Linux）的服务器或家庭内网主机上。

---

## 目录
- [一、项目端口与网络说明](#一项目端口与网络说明)
- [二、家庭宽带无 80/443 端口解决方案（重点）](#二家庭宽带无-80443-端口解决方案重点)
  - [方案 A：公网 IP / DDNS + 路由器高位端口转发（最常用）](#方案-a公网-ip--ddns--路由器高位端口转发最常用)
  - [方案 B：Cloudflare Tunnel 穿透（免公网IP、免开放端口、原生 443 HTTPS）](#方案-bcloudflare-tunnel-穿透免公网ip免开放端口原生-443-https)
  - [家宽申请 SSL 证书特别注意（DNS 验证）](#家宽申请-ssl-证书特别注意dns-验证)
- [三、服务器基础环境准备](#三服务器基础环境准备)
- [四、方案一：Node 项目一体化托管（最简便、强烈推荐 ⭐）](#四方案一node-项目一体化托管最简便强烈推荐-)
- [五、方案二：前后端分离专业部署（Nginx 静态加速）](#五方案二前后端分离专业部署nginx-静态加速)
- [六、关键目录与权限配置（必读）](#六关键目录与权限配置必读)
- [七、常见故障排查（Troubleshooting）](#七常见故障排查troubleshooting)
- [八、日常运维与平滑更新](#八日常运维与平滑更新)

---

## 一、项目端口与网络说明

### 1. 默认端口
- **后端服务端口**：默认监听 **`3001`**（在 `server/index.js` 中定义：`process.env.PORT || 3001`）。
  - 如需修改端口，可在启动命令前指定环境变量，例如：`PORT=8080 node server/index.js`。
- **前端网络请求**：
  - 前端在 `src/services/api.ts` 中全部使用相对路径（`/api` 和 `/uploads`）。
  - **绝无任何写死的端口**：这意味着无论你通过什么自定义端口访问（例如 `8080`、`8443`、`28080`），前端都会自动向当前访问的端口发起请求，绝不会因端口非 80/443 而出现请求失败或跨域问题。

### 2. 项目架构
- **前端**：React 19 + Vite 8 + TailwindCSS 4（打包输出目录：`dist/`）
- **后端**：Node.js Express + `better-sqlite3`（原生 C++ 绑定），内置了对前端打包产物 `dist/` 的全自动静态托管与 SPA 路由兜底
- **静态附件存储**：`server/uploads/`（头像与工作台上传的文件）
- **数据库**：`server/data/workbench.db`（本地 SQLite 数据库文件，无需安装 MySQL，开箱自启）

---

## 二、家庭宽带无 80/443 端口解决方案（重点）

> **背景原因**：国内各大运营商（电信、联通、移动）对家庭宽带默认封禁了入站的 **`80`**（HTTP）和 **`443`**（HTTPS）端口。但**其他非标高位端口（如 8080、8443、20000~60000）均正常开放**。

针对家庭宽带环境，推荐以下两种部署访问方案：

### 方案 A：公网 IP / DDNS + 路由器高位端口转发（最常用）

如果你的家庭宽带有公网 IP（通过向电信/联通客服申请，或具备公网 IPv6）：

#### 步骤 1：在宝塔中绑定非标端口
在宝塔面板添加 Node 项目或 Nginx 站点时，域名一栏**直接填入带端口的域名**：
- 示例：`work.yourdomain.com:8443`（用于 HTTPS）或 `work.yourdomain.com:8080`（用于 HTTP）。
- 宝塔 Nginx 会自动配置 `listen 8443;` 或 `listen 8080;`。

#### 步骤 2：路由器配置端口转发（NAT / 虚拟服务器）
进入主路由器管理后台（如华硕、OpenWrt、爱快、TP-Link、小米等）：
1. 找到 **端口转发 / 虚拟服务器** 菜单；
2. 新增一条 TCP 端口转发规则：
   - **外部端口**：`8443`（外网访问所用的端口）
   - **内部 IP**：部署了宝塔的局域网主机 IP（如 `192.168.1.100`）
   - **内部端口**：`8443`（如果通过宝塔 Nginx）或直接填 `3001`（如果直接直连后端 Node）
   - **协议**：`TCP`

#### 步骤 3：配置 DDNS（动态域名解析）
家庭公网 IP 每隔几天或重启光猫后会改变，需配合 DDNS 工具：
- 在宝塔中安装 **DDNS-GO** 插件，或在路由器内配置 DDNS（支持阿里云解析、腾讯云 DNSPod、Cloudflare 等）；
- 将域名 `work.yourdomain.com` 动态解析到家里的公网 IP。

#### 步骤 4：外网访问
直接在浏览器输入带端口的地址即可访问：
```text
http://work.yourdomain.com:8080
# 或
https://work.yourdomain.com:8443
```

---

### 方案 B：Cloudflare Tunnel 穿透（免公网IP、免开放端口、原生 443 HTTPS）

如果你的宽带是**大内网（没有公网 IP）**，或者**不想在路由器暴露任何端口**，强烈推荐免费的 **Cloudflare Zero Trust (Tunnel)**：

1. 拥有一个域名，并将 DNS 免费托管在 Cloudflare；
2. 登录 Cloudflare 控制台 -> 点击左侧 **Zero Trust** -> **Networks** -> **Tunnels**；
3. 点击 **Add a tunnel**，选择 Cloudflared 连接方式；
4. 复制面板给出的 Docker 或 Linux 命令，在宝塔主机的终端中直接粘贴运行；
5. 在 Cloudflare 网页端的 **Public Hostname** 中配置路由：
   - **Domain**：`work.yourdomain.com`
   - **Service Type**：`HTTP`
   - **URL**：`localhost:3001`（直接映射到 Workbench 项目端口）
6. **极致优势**：
   - **免端口直接访问**：直接访问 `https://work.yourdomain.com`（标准 443 端口，无需输入任何尾巴端口）；
   - **自动自带合法 SSL 证书**；
   - 无需公网 IP，无需配置光猫桥接和路由器端口映射。

---

### 家宽申请 SSL 证书特别注意（DNS 验证）

由于家庭宽带 80 端口被封禁，**不能使用**常规的 HTTP-01 文件验证申请 SSL 证书（会超时报错）。

**必须使用 DNS 验证方式申请**：
1. 打开宝塔面板「网站」-> 对应项目 ->「设置」->「SSL」；
2. 选择 **Let's Encrypt**，验证方式选择 **DNS 验证**；
3. 选择你的域名提供商（如阿里云 DNS / 腾讯云 DNSPod / Cloudflare），填入对应的 API AccessKey / Token；
4. 宝塔将通过 API 自动添加 TXT 记录完成域名验证并签发 SSL 证书；
5. 签发后开启 SSL，并在路由器将外部高位端口映射给内部的 HTTPS 端口（如外部 `8443` -> 内部 `8443`）。

---

## 三、服务器基础环境准备

### 1. 安装宝塔必要软件
登录宝塔 Linux 面板，前往 **「软件商店」**，安装以下核心软件：
1. **Nginx**：推荐安装 `Nginx 1.22` 或 `1.24`
2. **Node.js 版本管理器**：
   - 安装完成后打开插件，进入 **版本列表**；
   - 安装 **Node.js v20.x LTS**（或 v18.x）；
   - 在右侧设为 **命令行版本**（保证全局执行 `node -v`、`npm -v` 生效）。
3. **PM2 管理器**（可选，若使用宝塔新版自带的“Node 项目”管理器则已内置）。

### 2. 安装 C++ 编译环境（针对 better-sqlite3）
`better-sqlite3` 包含底层 C++ 拓展，在安装依赖时需要服务器具备基础编译环境。通过宝塔面板 **「终端」** 执行对应系统的命令：

- **Ubuntu / Debian**：
  ```bash
  sudo apt-get update
  sudo apt-get install -y build-essential python3 make gcc g++
  ```

- **CentOS / Rocky / AlmaLinux / RHEL**：
  ```bash
  sudo yum groupinstall -y "Development Tools"
  sudo yum install -y python3 make gcc gcc-c++
  ```

---

## 四、方案一：Node 项目一体化托管（最简便、强烈推荐 ⭐）

系统后端内置了对前端打包产物 `dist/` 的全自动托管与 SPA 路由兜底。**仅需启动一个 Node 进程，宝塔自动配置 Nginx 反代，一键完成全站部署**。

### 步骤 1：获取项目代码
在服务器终端中进入网站目录（例如 `/www/wwwroot/`），克隆代码：
```bash
cd /www/wwwroot
git clone https://github.com/wwenxiong/Workbench.git workbench
cd workbench
```
*(如果是本地代码上传：可将本地代码打包为 zip，上传至 `/www/wwwroot/workbench` 并解压)*

### 步骤 2：安装依赖并构建前端
```bash
# 1. 安装项目所有依赖
npm install

# 2. 构建前端静态资源（生成 dist 文件夹）
npm run build
```

> **提示**：如果服务器/NAS 内存较小（如 1G 内存）导致 `npm run build` 报错，可在本地电脑先执行 `npm run build`，将本地生成的 `dist` 目录直接上传到服务器的项目根目录下。

### 步骤 3：在宝塔中创建 Node 项目
1. 打开宝塔面板，进入 **「网站」** -> **「Node 项目」**。
2. 点击 **「添加 Node 项目」**，按如下信息填写：
   - **项目名称**：`workbench`
   - **项目目录**：选择 `/www/wwwroot/workbench`
   - **启动选项**：`node`
   - **启动文件/脚本**：`server/index.js`
   - **项目端口**：`3001`
   - **Node 版本**：选择安装的 `v20.x` 或 `v18.x`
   - **运行用户**：`www`（建议赋予 www 用户运行权限）
   - **绑定域名**：填入你的域名与端口（例如 `work.yourdomain.com:8080`，或内网 IP 如 `192.168.1.100:8080`）
3. 勾选 **「开启外网映射」**（宝塔会自动为您创建 Nginx 反向代理配置）。
4. 点击 **「确定」** 启动项目。

### 步骤 4：验证访问
浏览器打开 `http://你的域名或内网IP:端口` 即可直接访问工作台，系统已自动代理静态页面、API 请求和文件上传。

---

## 五、方案二：前后端分离专业部署（Nginx 静态加速）

如果需要利用 Nginx 达到极致的静态文件读取性能与缓存加速，可采用前后端分离架构。

### 步骤 1：构建前端并启动后端
1. 在 `/www/wwwroot/workbench` 下执行构建：
   ```bash
   npm run build
   ```
2. 使用 PM2 守护后端进程：
   ```bash
   pm2 start server/index.js --name "workbench-api"
   pm2 save
   pm2 startup
   ```

### 步骤 2：宝塔新建纯静态站点
1. 在宝塔面板进入 **「网站」** -> **「PHP 项目」/「HTML 项目」**，点击 **「添加站点」**。
2. **域名**：填入你的访问域名和端口（如 `work.yourdomain.com:8080`）。
3. **根目录**：选择前端打包产物目录 `/www/wwwroot/workbench/dist`。
4. 点击确定完成创建。

### 步骤 3：配置 Nginx 伪静态（解决 SPA 页面刷新 404）
点击该站点的 **「设置」** -> **「伪静态」**，填入以下规则并保存：
```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

### 步骤 4：配置反向代理（转发 API 和上传附件）
点击该站点的 **「设置」** -> **「配置文件」**，在 `server { ... }` 块内加入以下后端代理配置：

```nginx
    # 允许上传的最大文件大小（与系统 100MB 附件限制相匹配）
    client_max_body_size 100m;

    # 后端接口反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:3001/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 上传附件与头像访问代理
    location /uploads/ {
        proxy_pass http://127.0.0.1:3001/uploads/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
```
保存配置后，在宝塔中重载 Nginx 配置即可生效。

---

## 六、关键目录与权限配置（必读）

系统在运行时需要对数据目录及上传目录有读写权限：
- 数据库目录：`/www/wwwroot/workbench/server/data`
- 附件上传目录：`/www/wwwroot/workbench/server/uploads`

请在宝塔终端执行以下命令，确保 `www` 用户具备读写权限：
```bash
cd /www/wwwroot/workbench
mkdir -p server/data server/uploads/avatars
chown -R www:www server/data server/uploads
chmod -R 775 server/data server/uploads
```

---

## 七、常见故障排查（Troubleshooting）

### 1. 家宽环境下域名能解析但打不开
- **排查 1**：检查路由器端口映射是否生效。确保外部端口正确转发到内网宝塔主机的 IP 与端口。
- **排查 2**：检查宝塔防火墙以及系统防火墙（`ufw` 或 `firewalld`）是否放行了该端口（如 `8080`、`8443`）。
- **排查 3**：运营商可能封禁了部分常见端口（如 8080 偶尔也被封），可尝试换用更高位的不常见端口（如 `23001`、`38080` 等）。

### 2. `npm install` 时 better-sqlite3 报错或 `node-gyp rebuild` 失败
- **原因**：系统缺少 gcc/g++ 或 python 环境，或 Node.js 版本与预编译二进制不匹配。
- **解决办法**：
  1. 确保安装了开发工具包（`build-essential` 或 `Development Tools`）；
  2. 尝试切换 Node.js 为 **v20.x LTS**；
  3. 执行重新构建：
     ```bash
     npm rebuild better-sqlite3
     ```

### 3. 上传大文件时提示 `413 Request Entity Too Large`
- **原因**：Nginx 默认限制客户端请求体大小。
- **解决办法**：
  在站点 Nginx 配置的 `server` 块中加入：`client_max_body_size 100m;`，保存并重载 Nginx。

### 4. 打开页面空白，或刷新特定子路由报 404
- **原因**：前端是单页应用（SPA），刷新时 Nginx 找不到该子路径对应的物理文件。
- **解决办法**：
  在伪静态或站点 Nginx 配置中确保包含：
  ```nginx
  location / {
      try_files $uri $uri/ /index.html;
  }
  ```

### 5. 数据库报错 `SQLITE_READONLY` 或 `EACCES: permission denied`
- **原因**：Node 进程没有权限向 `server/data/workbench.db` 写入数据。
- **解决办法**：
  执行 `chown -R www:www server/data && chmod -R 775 server/data`。

---

## 八、日常运维与平滑更新

后续项目迭代更新时，只需要在终端中运行以下 3 步：

```bash
cd /www/wwwroot/workbench

# 1. 拉取 GitHub 最新代码
git pull origin main

# 2. 如果有新依赖则安装，并重新构建前端
npm install
npm run build

# 3. 重启后端服务
# 若使用宝塔 Node 项目：在宝塔面板对应项目点击「重启」
# 若使用 PM2：
pm2 restart workbench-api
```

---
> 如遇特殊环境问题，可在 GitHub 仓库提 Issue 进行反馈。
