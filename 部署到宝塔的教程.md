# Workbench 工作台 - 宝塔面板（BT-Panel）部署教程

本文档介绍如何将 **Workbench 个人工作台**（React 19 前端 + Node.js Express 后端 + SQLite 数据库）快速部署到安装了宝塔面板（Linux）的服务器上。

---

## 目录
- [一、项目架构与环境要求](#一项目架构与环境要求)
- [二、服务器基础环境准备](#二服务器基础环境准备)
- [三、方案一：Node 项目一体化托管（最简便、强烈推荐）](#三方案一node-项目一体化托管最简便强烈推荐)
- [四、方案二：前后端分离专业部署（Nginx 静态加速）](#四方案二前后端分离专业部署nginx-静态加速)
- [五、关键目录与权限配置（必读）](#五关键目录与权限配置必读)
- [六、配置域名与 SSL 证书（HTTPS）](#六配置域名与-ssl-证书https)
- [七、常见故障排查（Troubleshooting）](#七常见故障排查troubleshooting)
- [八、日常运维与平滑更新](#八日常运维与平滑更新)

---

## 一、项目架构与环境要求

- **前端**：React 19 + Vite 8 + TailwindCSS 4（打包输出目录：dist/）
- **后端**：Node.js Express + etter-sqlite3（原生 C++ 绑定），默认端口 3001
- **静态资源上传**：server/uploads/（头像与工作附件存储目录）
- **数据库**：server/data/workbench.db（SQLite 本地数据库，开箱自启，无需外置 MySQL/PostgreSQL）

### 服务器推荐配置
- **操作系统**：CentOS 7/8/9、Ubuntu 20.04/22.04/24.04 或 Debian 11/12
- **内存**：≥ 1GB（若服务器内存 ≤ 1GB，建议在宝塔“Linux 工具箱”中添加 1~2GB Swap 虚拟内存，避免前端构建时内存不足溢出）
- **Node.js**：**Node 18.x** 或 **Node 20.x LTS**（必须支持 ES Modules 及更好的 C++ 拓展兼容性）

---

## 二、服务器基础环境准备

### 1. 安装宝塔必要软件
登录宝塔 Linux 面板，前往 **「软件商店」**，安装以下核心软件：
1. **Nginx**：推荐安装 Nginx 1.22 或 1.24
2. **Node.js 版本管理器**：
   - 安装完成后打开插件，进入 **版本列表**；
   - 安装 **Node.js v20.x LTS**（或 v18.x）；
   - 在右侧设为 **命令行版本**（保证全局执行 
ode -v、
pm -v 生效）。
3. **PM2 管理器**（可选，若使用宝塔新版自带的“Node 项目”管理器则已内置）。

### 2. 安装 C++ 编译环境（针对 better-sqlite3）
etter-sqlite3 包含底层 C++ 拓展，在安装依赖时需要服务器具备基础编译环境。通过宝塔面板 **「终端」** 执行对应系统的命令：

- **Ubuntu / Debian**：
  `ash
  sudo apt-get update
  sudo apt-get install -y build-essential python3 make gcc g++
  `

- **CentOS / Rocky / AlmaLinux / RHEL**：
  `ash
  sudo yum groupinstall -y Development Tools
  sudo yum install -y python3 make gcc gcc-c++
  `

---

## 三、方案一：Node 项目一体化托管（最简便、强烈推荐）

系统后端内置了对前端打包产物 dist/ 的全自动托管与 SPA 路由兜底。**仅需启动一个 Node 进程，宝塔自动配置 Nginx 反代，一键完成全站部署**。

### 步骤 1：获取项目代码
在服务器终端中进入网站目录（例如 /www/wwwroot/），克隆代码：
`ash
cd /www/wwwroot
git clone https://github.com/wwenxiong/Workbench.git workbench
cd workbench
`
*(如果是本地代码上传：可将本地代码打包为 zip，上传至 /www/wwwroot/workbench 并解压)*

### 步骤 2：安装依赖并构建前端
`ash
# 1. 安装项目所有依赖
npm install

# 2. 构建前端静态资源（生成 dist 文件夹）
npm run build
`

> **提示**：如果服务器内存较小导致 
pm run build 报错，可在本地先运行 
pm run build，将本地生成的 dist 目录直接上传到服务器的项目根目录下。

### 步骤 3：在宝塔中创建 Node 项目
1. 打开宝塔面板，进入 **「网站」** -> **「Node 项目」**。
2. 点击 **「添加 Node 项目」**，按如下信息填写：
   - **项目名称**：workbench
   - **项目目录**：选择 /www/wwwroot/workbench
   - **启动选项**：
ode
   - **启动文件/脚本**：server/index.js
   - **项目端口**：3001
   - **Node 版本**：选择安装的 20.x 或 18.x
   - **运行用户**：www（建议赋予 www 用户运行权限）
   - **绑定域名**：填入你的域名（例如 work.yourdomain.com，未解析前可填服务器公网 IP）
3. 勾选 **「开启外网映射」**（宝塔会自动为您创建 Nginx 反向代理配置）。
4. 点击 **「确定」** 启动项目。

### 步骤 4：验证访问
浏览器打开 http://你的域名 即可直接访问工作台，系统已自动代理静态页面、API 请求和文件上传。

---

## 四、方案二：前后端分离专业部署（Nginx 静态加速）

如果需要利用 Nginx 达到极致的静态文件读取性能与 CDN 加速，可采用经典的前后端分离架构。

### 步骤 1：构建前端并启动后端
1. 在 /www/wwwroot/workbench 下执行构建：
   `ash
   npm run build
   `
2. 使用 PM2 守护后端进程：
   `ash
   pm2 start server/index.js --name workbench-api
   pm2 save
   pm2 startup
   `

### 步骤 2：宝塔新建纯静态站点
1. 在宝塔面板进入 **「网站」** -> **「PHP 项目」/「HTML 项目」**，点击 **「添加站点」**。
2. **域名**：填入你的访问域名（如 work.yourdomain.com）。
3. **根目录**：选择前端打包产物目录 /www/wwwroot/workbench/dist。
4. 点击确定完成创建。

### 步骤 3：配置 Nginx 伪静态（解决 SPA 页面刷新 404）
点击该站点的 **「设置」** -> **「伪静态」**，填入以下规则并保存：
`
ginx
location / {
  try_files  / /index.html;
}
`

### 步骤 4：配置反向代理（转发 API 和上传附件）
点击该站点的 **「设置」** -> **「配置文件」**，在 server { ... } 块内加入以下后端代理配置：

`
ginx
    # 允许上传的最大文件大小（与系统 100MB 附件限制相匹配）
    client_max_body_size 100m;

    # 后端接口反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:3001/api/;
        proxy_set_header Host System.Management.Automation.Internal.Host.InternalHost;
        proxy_set_header X-Real-IP ;
        proxy_set_header X-Forwarded-For ;
        proxy_set_header X-Forwarded-Proto ;
    }

    # 上传附件与头像访问代理
    location /uploads/ {
        proxy_pass http://127.0.0.1:3001/uploads/;
        proxy_set_header Host System.Management.Automation.Internal.Host.InternalHost;
        proxy_set_header X-Real-IP ;
        proxy_set_header X-Forwarded-For ;
    }
`
保存配置后，在宝塔中重载 Nginx 配置即可生效。

---

## 五、关键目录与权限配置（必读）

系统在运行时需要对数据目录及上传目录有读写权限：
- 数据库目录：/www/wwwroot/workbench/server/data
- 附件上传目录：/www/wwwroot/workbench/server/uploads

请在宝塔终端执行以下命令，确保 www 用户具备读写权限：
`ash
cd /www/wwwroot/workbench
mkdir -p server/data server/uploads/avatars
chown -R www:www server/data server/uploads
chmod -R 775 server/data server/uploads
`

---

## 六、配置域名与 SSL 证书（HTTPS）

为了保证密码加密传输与现代浏览器 API（如快捷剪贴板、离线提醒）正常工作，强烈推荐启用 HTTPS：

1. 在你的域名服务商后台，将域名 A 记录解析到服务器公网 IP。
2. 宝塔面板进入 **「网站」** -> 找到对应项目 -> 点击 **「设置」**。
3. 点击左侧 **「SSL」** 标签：
   - 选择 **Let's Encrypt** 或商用证书；
   - 勾选域名，点击 **申请**；
   - 申请成功后，打开右上角的 **「强制 HTTPS」** 开关。

---

## 七、常见故障排查（Troubleshooting）

### 1. 
pm install 时 better-sqlite3 报错或 
ode-gyp rebuild 失败
- **原因**：系统缺少 gcc/g++ 或 python 环境，或 Node.js 版本与预编译二进制不匹配。
- **解决办法**：
  1. 确保安装了开发工具包（uild-essential 或 Development Tools）；
  2. 尝试切换 Node.js 为 **v20.x LTS**；
  3. 执行重新构建：
     `ash
     npm rebuild better-sqlite3
     `

### 2. 上传大文件时提示 413 Request Entity Too Large
- **原因**：Nginx 默认限制客户端请求体大小为 1MB~50MB。
- **解决办法**：
  在宝塔站点设置中的 **「配置文件」** 或 Nginx 主配置的 http 块中加入：
  `
ginx
  client_max_body_size 100m;
  `
  保存并重载 Nginx。

### 3. 打开页面空白，或刷新特定子路由报 404
- **原因**：前端是单页应用（SPA），刷新时 Nginx 找不到该子路径对应的物理文件。
- **解决办法**：
  在伪静态或站点 Nginx 配置中确保包含：
  `
ginx
  location / {
      try_files  / /index.html;
  }
  `

### 4. 数据库报错 SQLITE_READONLY 或 EACCES: permission denied
- **原因**：Node 进程没有权限向 server/data/workbench.db 写入数据。
- **解决办法**：
  执行 chown -R www:www server/data && chmod -R 775 server/data。

### 5. 提示 Port 3001 is already in use
- **原因**：有旧的 Node 进程残留占用 3001 端口。
- **解决办法**：
  `ash
  # 查询占用 3001 端口的 PID
  lsof -i:3001
  # 或者
  netstat -tunlp | grep 3001
  # 杀死该进程
  kill -9 <PID>
  `

---

## 八、日常运维与平滑更新

后续项目迭代更新时，只需要在终端中运行以下 3 步：

`ash
cd /www/wwwroot/workbench

# 1. 拉取 GitHub 最新代码
git pull origin main

# 2. 如果有新依赖则安装，并重新构建前端
npm install
npm run build

# 3. 重启后端服务
# 若使用宝塔 Node 项目：在宝塔面板对应项目点击「重启」
# 若使用 PM2：
pm2 restart workbench-api
`

---
> 如遇特殊环境问题，可在 GitHub 仓库提 Issue 进行反馈。
